# 画面設計: 保護者ホーム（紐づけ園児一覧）

## 目的

- 紐づいている園児一覧を表示し、写真閲覧導線の起点にする。

## ルート

- `GET /guardian`

## 使用API

1. `GET /guardian/children`

## UI構成

- 園児カード一覧（氏名、組、園名、ラベル）
- 「写真を見る」ボタン（園児フィルタ付きで一覧へ遷移）
- 「招待QRで兄弟姉妹を追加」導線（/invite/:token を案内）

## 挙動

- 園児0件時は空状態を表示し、招待URLアクセス方法を案内

## エラー処理

- `401`: ログインへ
